query GetCharacters($page: Int!, $gender: String!) {
  characters(page: $page, filter: { gender: $gender }) {
    results {
      id
      name
      status
      species
      type
      gender
      origin {
        name
      }
      location {
        name
      }
      image
      created
    }
  }
}

